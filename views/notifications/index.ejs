<%- include ("../partials/header") %>

<h1>Notifications</h1>

<ul>
	<% allNotifications.forEach(function(notification) { %>
	<li>
		<a href="/restaurants/<%= notification.restaurantId %>">
			<%= notification.username %> created a new restaurant %>
		</a>
	</li>
	<% }); %>
</ul>

<%- include("../partials/footer") %>